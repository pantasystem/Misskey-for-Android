<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:orientation="horizontal"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content" android:id="@+id/reaction_counter_view">
    <ImageButton
            android:layout_width="30dp"
            android:layout_height="30dp" app:srcCompat="@drawable/reaction_icon_hmm"
            android:id="@+id/reaction_image_button" android:scaleType="centerCrop"
            android:padding="2dp"
            style="?android:attr/borderlessButtonStyle"/>
    <TextView
            android:text="@string/text_r"
            android:layout_width="wrap_content"
            android:layout_marginTop="2dp"
            android:layout_marginStart="2dp"
            android:layout_height="wrap_content" android:textSize="20sp" android:id="@+id/reaction_type_string_view"
            android:textStyle="normal"/>
    <TextView
            android:text="@string/number_0"
            android:layout_width="wrap_content"
            android:layout_marginTop="2dp"
            android:layout_marginStart="2dp"
            android:layout_height="wrap_content" android:textSize="20sp" android:id="@+id/reaction_count"/>

</LinearLayout>